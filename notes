in /lib/cryptsetup, there is an askpass program that takes a prompt as an argument and display the prompt but might do something else
there also is a script folder with some decrypt programs and a passdev program
folder run lock whoopsie but not accessible if not superuser
in var/www there are 2 folders level04 and level12